package com.example.deportestr.ui.models

import androidx.annotation.DrawableRes

data class Sport(
    //Modelo de deporte es lo mismo que hacer una entidad en java
    var id : Int,
    var name : String,
)
